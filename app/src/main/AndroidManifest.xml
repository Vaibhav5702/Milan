<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.milan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Milan">
        <activity android:name=".AnonymousCall"></activity>
        <activity android:name=".RestrictedSection.CreateRestrictedActivity" />
        <activity android:name=".AnonymousSection.CreateAnonymousRoom" />
        <activity android:name=".InterestSection.CreateInterestRoom" />
        <activity android:name=".LoginRegister.VerificationActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".LoginRegister.InterestActivity" />
        <activity android:name=".LoginRegister.SignUpActivity" />
        <activity android:name=".LoginRegister.LoginActivity" />
        <activity android:name=".LoginRegister.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>